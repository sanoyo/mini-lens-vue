import Vue from 'vue'
import Vuex from 'vuex'

import pod from '@/store/modules/pod'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    pod,
  }
})
